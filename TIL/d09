운영체제 문제

CPU 스케줄링
        기법            설명                                          문제해결
        (A)    먼저 들어온 프로세스 먼저 처리                      Convery Effect 발생
비선점   (B)    처리시간이 짧은 프로세스부터 처리                    starvation 발생
        HRN    짧은 작업시간이면서 대기시간이 긴 프로세스부터 처리    starvation 해결

        (C)    먼저 들어온 순서대로 일정 시간만큼만 처리
선점    (D)    남은 시간이 짧은 프로세스부터 처리
        MLQ    우선순위별로 큐를 분리하여 다양한 스케줄링 적용        starvation 발생

정답
(A) : FCFS
(B) : SJF
(C) : Round Robin
(D) : SRT
